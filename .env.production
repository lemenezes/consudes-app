# Variáveis de ambiente — build de produção (Vite)
# Este arquivo é commitado — conter apenas valores públicos

# CDN Cloudflare R2 — bucket: consudes-assets
VITE_MEDIA_BASE_URL=https://cdn.consudes.leandrom.com.br

# Cloudflare Worker — upload de fotos de galeria para R2
VITE_GALLERY_UPLOAD_ENDPOINT=https://consudes-upload-gallery-image.consudes-upload.workers.dev
